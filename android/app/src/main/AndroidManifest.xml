<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Network permissions - BẮT BUỘC cho streaming -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Audio permission - BẮT BUỘC cho mic/game audio -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    
    <!-- Foreground service - BẮT BUỘC để stream ổn định -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    
    <!-- SCHEDULE_EXACT_ALARM - BẮT BUỘC cho crash recovery restart -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    
    <!-- RECEIVE_BOOT_COMPLETED - BẮT BUỘC cho reboot recovery -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- BẮT BUỘC từ Android 14 khi dùng MediaProjection -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    
    <!-- BẮT BUỘC khi thu mic qua FGS -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    
    <!-- Keep CPU/network active - BẮT BUỘC khi màn hình tắt -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Notifications - BẮT BUỘC cho Android 13+ để hiện foreground notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Audio settings - TUỲ CHỌN nhưng NÊN CÓ để điều chỉnh audio routing -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    
    <!-- [PTL] Overlay permission - BẮT BUỘC cho floating overlay controls -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <!-- Screen capture: KHÔNG CẦN permission trong Manifest, dùng MediaProjection API -->
    
    <uses-sdk android:minSdkVersion="26" android:targetSdkVersion="34" />

    <application
        android:label="ScreenLive"
        android:name=".App"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">  <!-- [PTL] RTMPS only, no cleartext -->
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <!-- Foreground service for MediaProjection (Android 14+ compliant) -->
        <service
            android:name=".ScreenCaptureService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection|microphone"
            android:stopWithTask="false" />
        
        <!-- [PTL] Foreground service for floating overlay -->
        <service
            android:name=".overlay.OverlayService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection|microphone"
            android:stopWithTask="false" />
        
        <!-- [CRASH-RECOVERY] Receiver to restart stream after device reboot -->
        <receiver
            android:name=".BootCompletedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
